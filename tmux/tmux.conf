# Remove escape delay
set -sg escape-time 0

# Remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Easy config reload
bind-key r source-file ~/.tmux.conf \; display-message "tmux.conf reloaded."

# Window split keys
bind-key v split-window -h -c "#{pane_current_path}"
bind-key h split-window -v -c "#{pane_current_path}"

# Pane navigation (with prefix)
bind Left select-pane -L
bind Right select-pane -R
bind Up select-pane -U
bind Down select-pane -D

# Window navigation (Alt + arrows, no prefix)
bind -n M-Left previous-window
bind -n M-Right next-window

# Basic mouse support - only for scrolling and selecting panes
set -g mouse off
bind -n MouseDown1Pane select-pane -t=

# Allow terminal to handle all copy/paste
set-option -s set-clipboard on

# Better scroll behavior
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e'"

# Toggle nested session key bindings
bind -T root C-s  \
    set prefix None \;\
    set key-table off \;\
    set status off \;\
    set status-position top \;\
    refresh-client -S \;\

bind -T off C-s \
    set -u prefix \;\
    set -u key-table \;\
    set status on \;\
    set -u status-position \;\
    refresh-client -S

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Terminal settings
set-option -sa terminal-overrides ",xterm*:Tc"
set -g set-titles on
set -g set-titles-string '#H ❐ #S'

# Activity monitoring
setw -g monitor-activity on
set -g visual-activity on

# History
set -g history-limit 10000

# Window naming
set -g allow-rename off
bind c new-window -c "#{pane_current_path}"
bind , command-prompt -I "#W" "rename-window '%%'"

# Status bar and window customization (Nord theme)
set -g status-style "bg=#2E3440,fg=#D8DEE9"
set -g status-left "#[fg=#81A1C1,bg=#2E3440] « #S » "
set -g status-right ""
set -g status-justify left
set -g window-status-current-format "#[fg=#88C0D0,bold,bg=#2E3440] #I #[fg=#ECEFF4,bold]#{?window_name,#W,#{pane_current_command}} "
set -g window-status-format "#[fg=#4C566A,bg=#2E3440] #I #{?window_name,#W,#{pane_current_command}} "
